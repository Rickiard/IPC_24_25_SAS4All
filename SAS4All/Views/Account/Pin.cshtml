<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <title>Perfil com Abas</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Bootstrap JS Bundle (com Popper incluído) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <style>
        body {
            background-color: #f8f9fa;
            font-family: Arial, sans-serif;
        }

        .perfil-box {
            max-width: 800px;
            margin: 40px auto;
            background-color: #fff;
            border: 1px solid #00bfff;
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
            position: relative;
        }

        .perfil-icon-img {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            border: 2px solid #00bfff;
            background-color: white;
            padding: 4px;
            z-index: 1;
        }

        .user-title {
            text-align: left;
            margin-bottom: 10px;
        }

            .user-title strong {
                display: block;
            }

        .nav-tabs .nav-link.active {
            background-color: #00bfff;
            color: white;
            font-weight: bold;
        }

        .tab-content {
            margin-top: 20px;
            text-align: center;
        }
    </style>
</head>

<body>
        <br />
        <div class="d-flex flex-column align-items-center">
            <!-- Círculo azul -->
            <div class="bg-info rounded-circle" style="width: 30px; height: 30px;"></div>

            <!-- Linha vertical azul -->
            <div style="width: 2px; height: 40px; background-color: #00B4E6;"></div>

            <!-- Formulário com label, input e check -->
            <div class="d-flex align-items-center mt-3">
                <label for="pin" class="me-2 small">PIN ATUAL</label>
            <input type="text" id="pin" class="form-control form-control-sm me-2" placeholder="Pin Atual" style="width: 150px; border: 1.5px solid #444;">
                <!-- Botão com check estilizado -->
                <button class="btn btn-dark" type="button" style="background-color: transparent; border: none; font-size: 24px; color: black; line-height: 1;">
                    &#10003;
                </button>
            </div>
        </div>
</body>

</html>

